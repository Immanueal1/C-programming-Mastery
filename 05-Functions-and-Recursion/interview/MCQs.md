# 📝 Top 10 Multiple Choice Questions (MCQs)

> Test your understanding of Chapter 5 concepts with these technical multiple-choice questions.

---

### Question 1
Which function is considered the entry point of a C program?
- A) `start()`
- B) `init()`
- C) `main()`
- D) `execute()`

---

### Question 2
What is the default mechanism C uses to pass arguments to a function?
- A) Pass by Reference
- B) Pass by Value
- C) Pass by Pointer
- D) Pass by Name

---

### Question 3
If a function does not return any value, its return type should be:
- A) `int`
- B) `null`
- C) `empty`
- D) `void`

---

### Question 4
What will cause a recursive function to result in a Stack Overflow crash?
- A) Passing too many parameters
- B) Missing a Base Case
- C) Returning a `float` instead of an `int`
- D) Calling a library function inside it

---

### Question 5
The variables declared in a function definition header (e.g., `int x` in `void print(int x)`) are called:
- A) Actual Arguments
- B) Formal Parameters
- C) Global Variables
- D) Library Variables

---

### Question 6
Can `main()` call itself recursively in C?
- A) No, it throws a compilation error.
- B) No, it throws a runtime error.
- C) Yes, it is perfectly legal C syntax.
- D) Only if the return type is `void`.

---

### Question 7
In `result = pow(2, 3);`, what are `2` and `3`?
- A) Formal parameters
- B) Return types
- C) Actual arguments
- D) Prototypes

---

### Question 8
What is the primary advantage of writing functions?
- A) Makes the program run faster
- B) Reduces RAM consumption directly
- C) Enables code reusability and modularity
- D) Replaces the need for loops entirely

---

### Question 9
In `printf("%d", n);`, what is `printf`?
- A) User-defined function
- B) Operator
- C) Keyword
- D) Library function

---

### Question 10
How many values can a standard C function return using the `return` keyword per invocation?
- A) Zero
- B) One
- C) Two
- D) As many as parameters passed
