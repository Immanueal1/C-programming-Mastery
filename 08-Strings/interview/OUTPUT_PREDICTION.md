# 🔮 Top 10 Code Output Prediction Questions

> Predict the exact console output for the following C code snippets. Assume standard `#include <stdio.h>` and `#include <string.h>` setup for each exercise.

---

### Snippet 1
```c
char arr[] = "ABC"; 
printf("%ld", sizeof(arr));
```

---

### Snippet 2
```c
char s[10] = "Hi"; 
printf("%d", strlen(s));
```

---

### Snippet 3
```c
char s1[20] = "A"; 
char s2[] = "B"; 
strcat(s1, s2); 
puts(s1);
```

---

### Snippet 4
```c
char s[50] = "HelloWorld"; 
s[5] = '\0'; 
printf("%s", s);
```

---

### Snippet 5
```c
printf("%d", strcmp("Cat", "Cat"));
```

---

### Snippet 6
```c
char *p = "Hello"; 
p = "Bye"; 
printf("%s", p);
```

---

### Snippet 7
```c
char a[10] = "One"; 
strcpy(a, "Two"); 
printf("%s", a);
```

---

### Snippet 8
```c
char name[] = "Aman"; 
printf("%c", name[4]);
```

---

### Snippet 9
```c
char s[] = {'X', 'Y', 'Z', '\0'}; 
printf("%d", strlen(s));
```

---

### Snippet 10
```c
printf("%d", strcmp("Zebra", "Apple") > 0);
```
